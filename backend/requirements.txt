# Social Cook backend dependencies.
# Pinned floors for reproducibility; run `pip install -r requirements.txt`.
# (Run `pip freeze > requirements.lock.txt` after install to capture exact versions.)

# --- Web framework ---
fastapi>=0.111
uvicorn[standard]>=0.30

# --- Validation / settings ---
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1        # EmailStr validation for auth

# --- Database (Phase 2+) ---
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# --- Auth (Phase 2+) ---
passlib[argon2]>=1.7
PyJWT>=2.8
python-multipart>=0.0.9     # form parsing for OAuth2 password flow
slowapi>=0.1.9              # rate limiting for auth endpoints

# --- AI ---
anthropic>=0.34            # recipe structuring + vision (claude-sonnet-4-6)
openai>=1.40               # Whisper transcription (default Transcriber)

# --- HTTP / utilities ---
httpx>=0.27                # outbound HTTP (caption fetch, RevenueCat, etc.)

# --- Dev / test ---
pytest>=8.0
